<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.writerchainapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdventureActivity"></activity>
        <activity android:name=".ActionActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MysteryActivity" />
        <activity android:name=".TradgeyActivity" />
        <activity android:name=".WesternActivity" />
        <activity android:name=".CrimeActivity" />
        <activity android:name=".Genre2Activity" />
        <activity android:name=".ComedyActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChainRecyclerView" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GenreActivity" />
        <activity android:name=".utils.Help" />
        <activity android:name=".ui.login.HorrorActivity" />
        <activity android:name=".ui.login.LoveActivity" />
        <activity android:name=".ui.login.MedevilActivity" />
        <activity android:name=".ui.login.ScifiActivity" />
    </application>

</manifest>